const getAppeal = (req, res) => {
  res.status(200).send('GET endpoint exists but has not been implemented yet');
};

const postAppeal = (req, res) => {
  res.status(200).send('POST endpoint exists but has not been implemented yet');
};

const putAppeal = (req, res) => {
  res.status(200).send('PUT endpoint exists but has not been implemented yet');
};

const patchAppeal = (req, res) => {
  res.status(200).send('PATCH endpoint exists but has not been implemented yet');
};

module.exports = {
  getAppeal,
  postAppeal,
  putAppeal,
  patchAppeal,
};
