{% from "./document-link.njk" import documentLink %}

{% macro documentListLink(appealId, documents) %}
  <ul class="govuk-list">

    {% for document in documents %}
      <li>{{ documentLink(appealId, document) }}</li>
    {% endfor %}

  </ul>
{% endmacro %}
