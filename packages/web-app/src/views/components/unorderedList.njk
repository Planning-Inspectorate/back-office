{% macro unorderedList(items, rootUrl = '') %}
  <ul class="govuk-list">

    {% for item in items %}
      <li><a class="govuk-link" href="{{ rootUrl }}/{{ item.id }}">{{ item.name }}</a></li>
    {% endfor %}

  </ul>
{% endmacro %}
