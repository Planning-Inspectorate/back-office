{% from "components/button.njk" import button %}

{% macro buttonGroup(linkHref, linkText, buttonText) %}
  <div class="govuk-button-group">
    {{ button(buttonText) }}
    <a class="govuk-link" href="{{ linkHref }}">{{ linkText | default('Change outcome') }}</a>
  </div>
{% endmacro %}