{% from "govuk/components/button/macro.njk" import govukButton %}

{% macro form(content, method, buttonText) %}
  <form action="" method="{{ method | default('post') }}" novalidate>
    {{ content }}

    {{ govukButton({
      text: buttonText | default('Continue')
    }) }}
  </form>
{% endmacro %}