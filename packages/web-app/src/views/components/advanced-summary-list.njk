{% macro advancedSummaryList(data, titles, values, errors) %}
{% from "components/advanced-summary-list-row.njk" import advancedSummaryListRow %}

<dl class="govuk-summary-list">
  {% for title in titles %}
    {{advancedSummaryListRow(data[title], values, errors)}}
  {% endfor %}
</dl>

{% endmacro %}