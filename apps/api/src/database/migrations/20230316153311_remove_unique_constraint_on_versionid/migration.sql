BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Document] DROP CONSTRAINT [Document_versionId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
