BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[S51Advice] ALTER COLUMN [enquirer] NVARCHAR(1000) NULL;
ALTER TABLE [dbo].[S51Advice] ADD [firstName] NVARCHAR(1000),
[lastName] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
