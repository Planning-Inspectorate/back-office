BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ProjectUpdate] ADD [type] NVARCHAR(1000) NOT NULL CONSTRAINT [ProjectUpdate_type_df] DEFAULT 'general';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
