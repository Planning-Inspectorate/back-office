BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Folder] ADD [isCustom] BIT NOT NULL CONSTRAINT [Folder_isCustom_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
