{
	"name": "@pins/api-testing",
	"version": "1.0.0",
	"description": "API Tests for the Back Office Application",
	"main": "index.js",
	"scripts": {
		"start:test": "./api-test.sh",
		"schemas:clean": "rm -rf ./schemas",
		"api:test": "mocha 'tests/**/*.test.js'",
		"test:parallel": "mocha 'tests/**/*.test.js' --parallel --jobs 10",
		"init-test": "node utils/generate-schema.js"
	},
	"license": "ISC",
	"dependencies": {
		"ajv": "^8.12.0",
		"chai": "^4.3.7",
		"dotenv": "^16.0.3",
		"mocha": "^10.2.0",
		"supertest": "^6.3.3"
	},
	"type": "module"
}
